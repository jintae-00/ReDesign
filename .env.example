# ReDesign API keys — copy to `.env` and fill in your own values.
# The agent uses an OpenAI-compatible VLM router (OPENAI_API_KEY) and,
# optionally, Gemini for the nanobanana tool (GEMINI_API_KEY).
OPENAI_API_KEY=
GEMINI_API_KEY=
