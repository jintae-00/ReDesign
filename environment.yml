name: agent_qwen_layerd
channels:
  - defaults
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
dependencies:
  - python=3.12
  - pip=25.1
  - setuptools=78.1.1
  - wheel=0.45.1
  - pip:
      # ============================================================
      # Core ML/AI frameworks (pinned)
      # ============================================================
      - accelerate==1.12.0
      # diffusers is installed from git in post_install.sh (provides QwenImageLayeredPipeline)
      - transformers==4.57.3
      - tokenizers==0.22.1
      - safetensors==0.7.0
      - huggingface-hub==0.36.0
      - datasets==4.4.1
      - timm==1.0.17
      - einops==0.8.1
      - open-clip-torch==3.2.0
      - onnxruntime==1.23.2
      - ultralytics==8.3.166

      # ============================================================
      # LangChain / LangGraph ecosystem (pinned)
      # ============================================================
      - langchain==0.3.26
      - langchain-community==0.3.27
      - langchain-core==0.3.68
      - langchain-openai==0.3.27
      - langchain-text-splitters==0.3.8
      - langgraph==0.6.6
      - langgraph-checkpoint==2.1.1
      - langgraph-prebuilt==0.6.4
      - langgraph-sdk==0.2.2
      - langsmith==0.4.5

      # ============================================================
      # LLM API clients (pinned)
      # ============================================================
      - openai==1.95.1
      - google-genai==1.25.0
      - google-generativeai==0.8.5
      - tiktoken==0.9.0

      # ============================================================
      # PaddleOCR (pinned; paddlepaddle-gpu installed separately in post_install.sh)
      # ============================================================
      - paddleocr==3.1.0
      - paddlex==3.1.3
      - pyclipper
      - pypdfium2

      # ============================================================
      # Computer vision (pinned)
      # ============================================================
      - opencv-python
      - opencv-contrib-python
      - kornia==0.8.2
      - kornia-rs==0.1.10
      - scikit-image==0.25.2
      - supervision==0.25.1
      - lpips==0.1.4
      - pycocotools
      - simple-lama-inpainting

      # ============================================================
      # Data / scientific (unpinned - resolved automatically)
      # ============================================================
      - numpy
      - pandas
      - scipy
      - scikit-learn
      - matplotlib
      - pillow
      - imageio
      - imageio-ffmpeg
      - tifffile

      # ============================================================
      # Web / API frameworks
      # ============================================================
      - fastapi==0.116.1
      - uvicorn==0.35.0
      - gradio==5.36.2
      - gradio-client==1.10.4
      - starlette
      - httpx
      - httpcore
      - httpx-sse
      - aiohttp
      - aiofiles
      - websockets

      # ============================================================
      # Jupyter
      # ============================================================
      - jupyterlab
      - notebook
      - ipykernel
      - ipython

      # ============================================================
      # Utilities (unpinned)
      # ============================================================
      - packaging
      - pydantic
      - pydantic-core
      - pydantic-settings
      - pyyaml
      - omegaconf==2.3.0
      - hydra-core==1.3.2
      - jinja2
      - requests
      - certifi
      - urllib3
      - filelock
      - fsspec
      - tqdm
      - rich
      - click
      - typer
      - python-dotenv
      - python-multipart
      - orjson
      - regex
      - six
      - sympy
      - networkx
      - joblib
      - protobuf
      - grpcio
      - sqlalchemy

      # ============================================================
      # Document / file processing
      # ============================================================
      - openpyxl
      - xlsxwriter
      - python-pptx
      - beautifulsoup4
      - lxml
      - cairosvg
      - cairocffi
      - cssutils
      - premailer
      - psd-tools

      # ============================================================
      # Misc tools
      # ============================================================
      - playwright
      - vtracer
      - shapely
      - addict
      - astor
      - colorlog
      - coloredlogs
      - prettytable
      - lark
      - dataclasses-json
      - marshmallow
      - ruamel-yaml
      - tomlkit
      - ruff
      - yapf
      - gputil
      - psutil
      - py-cpuinfo
      - pyarrow
      - dill
      - multiprocess
      - xxhash
      - zstandard
      - brotli
      - chardet

      # ============================================================
      # Google API
      # ============================================================
      - google-api-core
      - google-api-python-client
      - google-auth
      - google-auth-httplib2
      - googleapis-common-protos
      - google-ai-generativelanguage

      # ============================================================
      # PySide6
      # ============================================================
      - pyside6==6.10.0

      # ============================================================
      # Misc (carried over from the original environment)
      # ============================================================
      - antlr4-python3-runtime==4.9.3
      - ffmpy
      - ftfy
      - greenlet
      - groovy
      - iopath
      - opt-einsum
      - portalocker
      - pulp
      - semantic-version
      - ujson
      - ormsgpack
      - flatbuffers
      - safehttpx
      - pydub
      - pyee
      - distro
      - hf-xet
      - jiter
      - sniffio

# ============================================================
# Installed separately (run after `conda env create`) — see post_install.sh:
# ============================================================
#
# 1. PyTorch (cu128 wheels; pulls pytorch-triton + nvidia-* automatically)
#    pip install --pre torch torchaudio torchvision --index-url https://download.pytorch.org/whl/nightly/cu129
#
# 2. PaddlePaddle GPU (not on PyPI; use the official index)
#    pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#
# 3. SAM 2 (not on PyPI; install from GitHub)
#    pip install git+https://github.com/facebookresearch/sam2.git
# ============================================================